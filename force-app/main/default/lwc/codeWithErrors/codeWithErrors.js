import { LightningElement } from 'lwc';

export default class CodeWithErrors extends LightningElement {

    handleError() {
        const my = new Date();
        const endDateTime = startDateTime.setMinutes(startDateTime.getMinutes() + CALL_DURATION_IN_MINUTES);
    }
}